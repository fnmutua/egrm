/**
 * Background worker placeholder — BullMQ/Redis queue removed for now.
 * Notifications dispatch inline in the API process.
 */
console.log('[worker] No background jobs configured yet (notifications run inline in API).');
